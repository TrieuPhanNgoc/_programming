find_package(Boost REQUIRED COMPONENTS log)
find_package(Threads REQUIRED)

set(THREADS_PREFER_PTHREAD_FLAG TRUE)

add_executable(async_pro _async_pro.cpp)

target_compile_options(async_pro PRIVATE -std=c++11 -Wall)

target_link_libraries(async_pro Boost::log ${CMAKE_THREAD_LIBS_INIT})